{
  "id": "auth-bad-password",
  "description": "Wrong password must not yield a session",
  "expect_success": false,
  "steps": [
    { "op": "authenticate", "legacy_auth": false, "password": "__invalid__" }
  ]
}
